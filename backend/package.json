{
  "name": "vulnstore-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend mock (Express) para VulnStore — sirve los endpoints /products, /auth/login y /checkout que consume la app RN/Expo. Uso educativo, no productivo.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "morgan": "^1.10.0",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.4.5"
  }
}
